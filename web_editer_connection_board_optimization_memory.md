# Web 编辑器连连看优化长期记忆文档

## 文档目的

这份文档用于长期记录 `web_editer` 连连看界面的优化方向、已认可判断、待讨论问题和落地顺序。

后续讨论中，只要某个方案被确认是优选方案，就追加到本文档的“决策记录”或对应章节中。本文档不直接替代旧表单模式，也不要求插件端或客户端组件改变协议；它主要约束 web 编辑器连连看界面的下一代交互设计。

## 当前核心判断

连连看界面应该成为“便携式资源装配工具”，而不是把旧表单里的所有控制器细项搬过来。旧表单可以继续保留完整控制器编辑能力，连连看则应该尽量让用户只表达业务意图。

控制器在连连看里应当被淡化。用户真正关心的是：

- 哪个模型是实体本体。
- 哪个模型是背饰或翅膀。
- 哪些模型是技能特效模型。
- 每个技能模型对应哪个技能轨道。
- 本体模型使用哪一套基础动画控制器。

用户不应该长期维护那些几乎不会改变的控制器细项，例如渲染器条件、动画播放条件、技能模型的控制器选择等。

## 已认可方向

### 1. 连连看去掉控制器条件编辑

连连看界面中应移除渲染控制器和动画控制器的条件选项。

原因：

- 当前控制器本身已经内置显示条件和动画播放条件。
- 技能模型必须在放技能时才渲染，这一点是长期不变的业务事实。
- 连连看作为新一代便携界面，不应该保留一个长期不用且容易误导用户的入口。
- 旧表单模式继续保留这些条件字段，作为兼容和高级调试入口。

### 2. 渲染器装配应从“选择控制器”转为“标记模型角色”

当前连连看仍然偏向“拖资源到某个渲染控制器槽位”。下一代连连看应该改成先标记模型角色。

模型角色建议分为：

- `body`：实体本体模型，必须有且只能有一个。
- `wings`：背饰或翅膀模型，可选。
- `skill`：技能特效模型，可以有多个。

渲染器轨道不应该要求用户逐个理解和选择。用户只需要确定：

- 哪个模型是本体。
- 哪个模型是背饰。
- 每个技能模型对应第几个技能轨道。

其余渲染器配置由编辑器根据角色自动生成。

### 3. 技能模型轨道应自动分配到 a 至 z

技能特效模型不需要用户再决定“使用第几个渲染器”。有几个技能模型，就按稳定顺序分配到 `a` 至 `z` 轨道。

用户真正需要确认的是“技能 1、技能 2、技能 3 分别对应哪个模型”，而不是控制器内部如何连。

建议规则：

- 本体模型固定使用 `default` 资源 key。
- 翅膀模型建议固定使用 `wings` 或独立命名，避免和技能轨道混淆。
- 技能模型按 `a`、`b`、`c`、... 分配资源 key。
- 动作槽位按轨道后缀生成，例如 `skill1A`、`skill2A`、`idleA`、`walkA`。

### 4. 动画控制器应由模型角色反推

模型角色确定后，动画控制器数量和大多数控制器选择其实已经可以确定。

规则建议：

- 本体模型的动画控制器需要用户确定，因为不同实体的基础行为差异最大。
- 技能模型的动画控制器根据该技能模型拥有的动作数量自动选择。
- 翅膀模型的动画控制器几乎固定为 `controller.animation.entity_idle_wings.default`。
- 系统缩放控制器仍然固定内置，不给用户编辑。

技能模型控制器选择不应该暴露成长期表单项。用户只需要把动作资源归到某个技能模型，编辑器即可知道该技能模型需要几段动作，再匹配对应轨道的控制器。

## 我的补充评估

### 1. 连连看应该维护“业务装配模型”，再生成旧数据结构

当前代码底层已经有：

- `renderControllers`
- `animationControllerBindings`
- `geometryMappings`
- `textureMappings`
- `animationMappings`

这些结构适合导出，但不适合直接作为下一代连连看的主要心智模型。

建议新增一层轻量的连连看业务装配状态，例如：

```js
connectionAssembly: {
  body: {
    geometryResourceId: "",
    textureResourceId: "",
    baseAnimationController: ""
  },
  wings: {
    geometryResourceId: "",
    textureResourceId: "",
    enabled: false
  },
  skills: [
    {
      trackKey: "a",
      skillIndex: 1,
      geometryResourceId: "",
      textureResourceId: "",
      animationNames: []
    }
  ]
}
```

这层结构只属于编辑器，用于表达用户在连连看里真正做了什么。导出前再把它编译成现有的 `renderControllers`、`animationControllerBindings`、`animate`、`render`、`client_entity.json`。

注意：这不是为了制造新抽象，而是为了把 UI 心智模型和导出协议分开。否则连连看会持续被底层控制器结构绑架。

### 2. 不建议完全删除旧的控制器结构

旧表单仍然应保留完整控制器结构。原因：

- 它是高级逃生口。
- 它方便兼容导入的复杂资源包。
- 它方便调试 manifest 未收录的新控制器。
- 它可以继续承载少数非标准配置。

连连看要做的是“默认不让用户碰复杂控制器”，而不是从系统里删掉控制器能力。

### 3. 自动生成必须是可预测的

连连看自动生成控制器和轨道时，规则必须稳定，不要做模糊猜测。

建议优先级：

1. 用户显式标记的角色和技能编号优先。
2. 已存在资源 key 其次，例如 `default`、`wings`、`a`、`b`。
3. 文件名推断最后使用，只能作为初始导入建议。

不要根据中文名字、动作数量或文件顺序做不可解释的强推断。文件顺序可以用于初始排序，但最终必须能在 UI 中明确展示和修改。

### 4. 技能动作数量到控制器的映射需要一张规则表

技能模型“有几个动作就选哪个控制器”需要明确规则表，否则后续很容易出现魔法逻辑。

建议维护类似规则：

| 轨道 | 动作数量 | 控制器 |
| --- | --- | --- |
| `a` | 1 | `controller.animation.entity_skill1_a.default` 或现有等价控制器 |
| `a` | 2 | `controller.animation.entity_skill2_a.default` 或现有等价控制器 |
| `a` | 5 | `controller.animation.entity_skill5_a.default` 或现有等价控制器 |
| `b` | 5 | `controller.animation.entity_skill5_b.default` 或现有等价控制器 |

如果当前 `use_controllers` 的命名不是这种形式，就先建立“动作数量 + 轨道 -> 控制器名”的映射函数，不要把规则散落在 UI 事件里。

### 5. 轨道字母和骨骼隔离应联动

当前已经有“动画控制器目标模型资源”的自动推断，例如 `skill1A -> a`。

下一代连连看如果能明确技能模型轨道，就应直接生成：

- 模型资源 key：`a`
- 动作 key：`skill1A`
- 动画目标模型资源：`a`
- 骨骼隔离改名：`ba_a_xxx`

这样能减少手动设置“目标模型资源”的机会。旧表单仍然保留手动覆盖。

### 6. 背饰或翅膀应作为特殊角色处理

翅膀不应该混入技能轨道，因为它通常是常驻外观，不是技能特效。

建议：

- UI 上单独标记为“背饰/翅膀”。
- 渲染控制器使用专用 wings 渲染逻辑。
- 动画控制器默认写死 `controller.animation.entity_idle_wings.default`。
- 动作槽位默认只需要 `idle_wings` 或控制器要求的固定槽位。

如果未来翅膀需要技能态或展开态，再作为背饰角色的高级能力扩展，不要一开始就混进技能模型系统。

## 推荐的下一代连连看交互

### 总体布局：分层全局图

连连看界面应该从当前“滚动条里找槽位”的形式，改成一个能一眼看完整体关系的分层图。

推荐分层：

1. 资源层：模型、贴图、动作资源。
2. 角色/轨道层：本体、翅膀、技能 1、技能 2、技能 3 等业务轨道。
3. 自动生成层：渲染器和动画控制器的生成结果。
4. 导出预览层：最终会写入 `geometry`、`texture`、`render`、`animate`、`animate_controller` 的结果。

其中用户主要操作资源层到角色/轨道层的连接。角色/轨道层之后的大部分线应该由编辑器自动生成，只做展示和诊断，不应该要求用户手动连一堆控制器线。

如果连线很多，界面应支持“局部放大编辑”。不放大时也可以连线，但放大后应提供更大的画布、更多标签和更清晰的端点命中区域。

### 第一步：资源导入和角色识别

用户导入资源包或拖入资源后，连连看展示资源卡片。

每个模型卡片可以选择角色：

- 本体
- 背饰/翅膀
- 技能模型
- 暂不使用

如果只有一个模型，默认标记为本体。

如果有多个模型，编辑器可以按 `default`、文件名、已有 client_entity geometry key 做初始建议，但必须允许用户改。

### 第二步：技能轨道分配

被标记为技能模型的资源进入技能轨道列表。

每个技能模型展示：

- 技能编号，例如技能 1。
- 轨道 key，例如 `a`。
- 绑定的模型资源。
- 绑定的贴图资源。
- 绑定的动作列表。

用户可以调整技能编号和轨道顺序。

### 第三步：本体动画控制器选择

本体模型保留一个简化选择：

- 基础行为控制器。
- 本体动作槽位映射，例如 idle、walk、skill1 等。

这一步是用户最可能需要手动判断的部分，因此不应该被完全自动隐藏。

### 第四步：技能动画自动生成

技能模型根据已绑定动作数量和轨道 key 自动生成动画控制器。

例如技能模型 `a` 有 5 个动作：

- 自动选择轨道 `a` 的五动作控制器。
- 自动生成 `skill1A` 到 `skill5A`。
- 自动设置这些动作的目标模型资源为 `a`。

如果无法找到对应控制器，UI 应提示“缺少轨道 a 的 5 动作控制器”，并给出旧表单模式入口。

### 第五步：导出前编译

导出时把连连看业务装配状态编译成现有结构：

- `renderControllers`
- `animationControllerBindings`
- `animate`
- `animate_controller`
- `client_entity.json`
- `animation.json`
- `geometry.json`

这样不改变插件和组件调用链，只改变编辑器内部组装体验。

## 暂不落地的内容

以下内容先记录，不立即实现：

- 完全移除旧表单。
- 修改插件端配置格式。
- 修改客户端组件套模型协议。
- 让用户编辑控制器源码。
- 用复杂 AI 猜测自动识别所有模型角色。

## 风险和边界

### 1. 导入复杂资源包时不能破坏原配置

资源包导入时，源文件不应该被修改。编辑器可以创建内部装配状态，但导入源文件必须保持只读。

导出时可以继续按编辑器规则规范化命名，这是当前已认可边界。

### 2. 旧表单和连连看可能产生双向同步问题

如果新增 `connectionAssembly`，需要决定旧表单修改后是否反推回连连看业务状态。

建议第一阶段只做单向：

- 连连看生成底层结构。
- 旧表单可以继续编辑底层结构。
- 一旦用户进入旧表单做高级编辑，连连看可以提示“当前实体包含高级控制器配置，自动装配预览可能不完全等价”。

不要在第一版做复杂双向同步，否则很容易出现状态互相覆盖。

### 3. 自动控制器选择依赖 manifest 完整性

技能模型自动选择控制器必须依赖 `controller-manifest.js` 或一份明确规则表。

如果 manifest 中没有对应控制器，不应该静默失败。应该在 UI 中明确提示缺少哪个轨道、几个动作的控制器。

## 初步落地顺序

1. 新增连连看业务装配状态，只用于新 UI，不影响旧表单。
2. 在连连看资源卡片上增加模型角色标记：本体、翅膀、技能、暂不使用。
3. 移除连连看中的渲染器条件和动画控制器条件入口，只在旧表单保留。
4. 做技能轨道分配 UI：技能编号、轨道 key、模型资源、贴图资源、动作列表。
5. 实现“业务装配状态 -> 现有 renderControllers / animationControllerBindings”的编译函数。
6. 接入技能动作数量到控制器的规则表。
7. 接入翅膀专用默认控制器。
8. 保留旧表单高级编辑入口，并在检测到高级配置时给出提示。

## 决策记录

### 2026-06-23：第三阶段开放安全的角色/轨道编辑入口

已落地方向：

- 连连看装配状态面板开始提供自动装配模式入口。
- `legacy` 模式仍然只镜像旧表单结构，不直接覆盖旧控制器。
- `assembly` 模式中可以编辑本体、翅膀和技能轨道。
- 本体可选择模型、贴图、基础动画控制器和动作归属。
- 翅膀可启用/关闭，并选择模型、贴图和动作归属。
- 技能轨道可新增、删除、选择模型、贴图、动作，多选动作后由集中编译函数生成 `skill1A` 这类槽位。
- 技能轨道支持轨道 key 和锁定状态，新增轨道优先选择未使用的 a-z。
- UI 事件只写 `connectionAssembly`，不直接散改 `renderControllers` 或 `animationControllerBindings`。

补充结论：

- 第三阶段已经具备“用户编辑业务装配状态 -> 导出前集中编译旧协议结构”的基础闭环。
- 当前仍未重做全局大画布，下一步可以开始考虑分层图和放大编辑体验。
- 自动装配模式存在阻断错误时不会污染旧底层控制器。

### 2026-06-23：第二阶段接入只读装配状态面板

已落地方向：

- 连连看界面新增只读“装配状态”面板，显示当前是旧表单兼容模式还是自动装配模式。
- 面板开始展示 `errors`、`warnings`、`infos` 三类诊断，为后续固定校验面板打底。
- 面板展示本体、翅膀、技能轨道摘要，但暂不开放切换到 `assembly` 模式。
- 暂不提供模式切换按钮，避免角色/轨道编辑 UI 尚未完成时让用户进入半成品状态。

补充结论：

- 第二阶段仍然遵守“先底座、后大画布”的原则。
- 当前 UI 只负责透明展示底座状态，不改变旧表单已有行为。
- 后续可以在此面板基础上继续扩展角色标记、技能轨道锁定和放大图入口。

### 2026-06-23：第一阶段先落地连连看底座，不重写大 UI

已落地方向：

- `connectionAssembly` 作为编辑器状态进入实体数据，带 `version` 和 `mode`。
- `legacy` 模式只镜像当前旧表单结构，不主动覆盖现有 `renderControllers` 和 `animationControllerBindings`。
- `assembly` 模式才会从业务装配状态集中编译到底层控制器结构。
- 导出前统一执行准备函数，保证未来新连连看状态能在导出前重新编译。
- 集中诊断函数已经拆成 `errors`、`warnings`、`infos`，后续 UI 校验面板直接消费。
- 复制实体时会重映射 `connectionAssembly` 内部资源 id，避免引用旧实体资源。

补充结论：

- 第一阶段的目标是建立稳定数据和编译链，不立即重写连线大界面。
- 旧表单仍然安全，当前不会因为新增 `connectionAssembly` 而被无条件覆盖。
- 后续新 UI 只需要把实体切到 `assembly` 模式并填充业务角色，即可复用集中编译链。

### 2026-06-23：自动布局、确认态和高级覆盖必须稳定

已确认方向：

- 连线图必须支持自动整理布局。导入资源后节点应按层自动排好，不要求用户手动摆节点。
- 放大图里可以允许用户微调节点位置，但普通视图不应该让用户维护布局。
- 每个轨道必须有明确状态，例如 `未完成`、`已自动生成`、`需要处理`、`高级覆盖中`。
- 技能轨道应该支持锁定。用户确认技能 1 使用轨道 `a` 后，重新导入或重新排序资源时不应打乱它。
- 资源识别和轨道识别要分清“建议”和“确认”。导入时可以自动建议角色，但用户确认后才视为正式装配。
- 高级覆盖必须可逆。用户手动改控制器后，连连看标记为“高级覆盖”，同时提供“恢复自动生成”入口。
- 导出前应提供装配摘要，用自然语言列出本体、翅膀、技能轨道、动作归属、自动控制器和异常。
- 动作资源不能和技能轨道强绑死。动作既可以归到本体动作，也可以归到技能轨道动作。
- 异常需要分级。缺本体模型、缺本体贴图、缺必要控制器属于阻止导出；未使用动作、未使用模型、高级覆盖、自动推断不完整属于警告。
- `connectionAssembly` 如果出现在导出后的文件中，必须以注释形式出现，不能变成插件端或客户端组件必须解析的新配置协议。

补充结论：

- 自动化只能做建议和生成，用户确认过的东西必须稳定。
- 用户手动覆盖过的东西必须能回到自动模式。
- 连连看的状态表达要优先于线条数量，用户应该先看轨道状态，再决定是否展开具体连线。
- `connectionAssembly` 是编辑器工程状态，不是运行时协议。导出文件可以用注释保留它，方便未来回导或排查，但业务功能必须仍然由现有 `geometry`、`texture`、`render`、`animate`、`animate_controller`、`entity_profile` 等字段承载。

### 2026-06-23：连连看只展示业务决策线，自动实现线默认折叠

已确认方向：

- 连线图不应画成真正的“全连接图”。参考图只用于表达分层，不代表要把所有实现关系都画出来。
- 主界面只画用户需要确认的线，例如 `资源 -> 角色/轨道`。
- 角色/轨道之后的渲染器、动画控制器、目标模型资源等实现线，默认折叠成自动生成说明。
- 自动生成结果必须可解释，例如“因为技能轨道 A 绑定了 5 个动作，所以选择某个 5 动作控制器”。
- 放大图不能维护第二套状态，它只是同一份连连看数据的大画布视图。
- 连连看需要固定校验面板，用于提示缺少本体模型、缺少贴图、轨道控制器缺失、动作未使用等问题。
- 控制器层只读为主，只有 manifest 缺失、动作数量无法匹配、用户切到高级模式时才允许手动覆盖。
- 性能上不应使用大量 DOM 线。节点可以继续用 DOM，连线建议用 SVG 或 Canvas 覆盖层绘制。
- 自动线默认聚合，点开节点或进入放大图后再展开局部线，避免大量线同时拖慢页面。

补充结论：

- 连连看不应该展示所有真实实现关系，而应该展示用户需要决策的业务关系。
- 实现关系只作为可解释的自动生成结果出现。
- 这条原则优先级高于“完整展示所有连接”，因为完整展示会把界面重新推回复杂表单。

### 2026-06-23：连线图应重做为全局分层图

已确认方向：

- 渲染控制器确定后，大部分动画控制器连线已经可以自动确定，不应该继续让用户手动连大量控制器线。
- 连连看界面当前缺少全局视野，不应该让用户长期靠滚动条一个个找槽位。
- 新界面应参考“分层网络图”的表达方式，把资源、业务轨道、自动控制器、导出结果拆成横向层。
- 默认视图要能一览全局；线多时允许点开局部放大图继续连线。
- 放大连线和不放大连线应同时存在，用户可以按复杂度选择。

补充结论：

- 控制器应放在靠后的“自动生成层”，默认折叠或弱化展示。
- 资源应放在最左侧“资源层”，按模型、贴图、动作分组。
- 用户主要连“资源 -> 角色/轨道”，而不是“资源 -> 控制器槽位”。
- 角色/轨道层是新连连看的核心层，它比控制器更符合用户心智。

### 2026-06-23：连连看应淡化控制器概念

已确认方向：

- 连连看不应长期暴露渲染器条件和动画控制器条件。
- 渲染器装配应转为模型角色识别：本体、翅膀、技能模型。
- 技能模型不再让用户选择第几个渲染器，而是按技能轨道自动生成。
- 本体动画控制器保留用户选择。
- 技能模型动画控制器按动作数量和轨道自动选择。
- 翅膀动画控制器默认固定为 `controller.animation.entity_idle_wings.default`。

补充结论：

- 下一代连连看建议新增轻量业务装配状态，再编译成旧的底层导出结构。
- 旧表单继续作为完整控制器编辑和高级调试入口。
- 自动推断规则必须稳定、可解释、可覆盖。
